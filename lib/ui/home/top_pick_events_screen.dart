import 'package:flutter/material.dart';

class TopPickEventsScreen extends StatelessWidget {
  const TopPickEventsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('top pick events screen'));
  }
}
